# Rust
/target
**/target
# Cargo.lock IS committed: TkSpeed is an application, not a library
# (reproducible builds — required for Alpha release A6).

# Node
node_modules
dist
.pnpm-store

# Bindings TS gerados pelo ts-rs (regerados a cada `cargo test`; tipos espelhados à mão no frontend)
crates/tk-contracts/bindings/

# Tauri
src-tauri/gen
src-tauri/target

# Build artifacts / installers (generated, never versioned)
*.exe
*.msi
*.zip

# Local data / runtime
*.db
*.db-wal
*.db-shm
.env
.DS_Store
logs/

# Editors / IDE
.vscode/
.idea/
*.swp

# Tauri signing keys (NEVER commit)
.tauri/tkspeed.key
.tauri/tkspeed.key.pub
